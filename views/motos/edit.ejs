<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>- Tienda de Motos -</title>
    <link rel="stylesheet" href="">
</head>
<body>
    <%- include('../partials/header') %>

   <div>
    <h1>Formulario de crecación de Moto</h1>
    <form action="/motos/<%=moto.id%>?_method=PUT" method="POST" enctype="multipart/form-data">

        <div>
            <label for="nombre">Nombre: </label>
            <input type="text" name="nombre" id="nombre" value="<%=moto.nombre%>" required>
        </div>

        <div>
            <label for="descripcion">Descripcion: </label>
            <textarea name="descripcion" id="descripcion" value="<%=moto.descripcion%>"  required></textarea>
        </div>

        <div>
            <label for="precio">Precio: </label>
            <input type="number" name="precio" id="precio" value="<%=moto.precio%>"  required>
        </div>

        <div>
            <label for="imagen">Imagen: </label>
            <input type="file" name="imagen" id="imagen">
            <p>Imagen actual: <img src="/images/<%=moto.imagen%>" alt="<%=moto.nombre%>"></p>
        </div>
        
        <div>
            <label for="categoria_id">Categoría: </label>
            <select name="categoria_id" id="categoria_id">
                <% categorias.forEach(categoria => { %>
                    <option value="<%=categoria.id%>"<%=moto.categoria_id===categoria.id ? 'selected' : ''%>><%=categoria.nombre%></option>
                <% }) %>
            </select>
        </div>
        <button type="submit">Actualizar Moto</button>

    </form>
   </div>

   <a href="/">Volver a Inicio</a>

     <%- include('../partials/footer') %>
</body>
</html>