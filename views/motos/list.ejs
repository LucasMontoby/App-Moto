<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Motos - Tienda de Motos -</title>
    <link rel="stylesheet" href="">
</head>
<body>
    <%- include('../partials/header') %>

    <div class="container">
        <h1>Listado de Motos</h1>
        <a href="/motos/create">Crear Motos</a>

        <div>
            <% motos.forEach(moto => { %>
                <div>
                    <img src="/images/<%=moto.imagen%>" alt="<%=moto.nombre%>">
                </div>
                <h2><%=moto.nombre%></h2>
                <p><%=moto.descripcion%></p>
                <p>$<%=moto.precio%></p>
                <p><strong>Categoría: </strong></p>
                <div>
                    <a href="/motos/<%=moto.id%>/edit">Editar</a>

                    <form action="/motos/<%=moto.id%>?_method=DELETE" method="POST">
                        <button type="submit">Eliminar</button>
                    </form>
                </div>

            <% }) %>
        </div>
        <a href="/">Volver a Inicio</a>

    </div>

     <%- include('../partials/footer') %>
</body>
</html>